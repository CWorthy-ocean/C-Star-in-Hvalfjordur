# C-Star in Hvalfjordur

## Contribution

After cloning the repository:

1. Create a python virtual environment
2. Install the requirements for building documentation
    - `pip install -r ./requirements.txt`
3. Run `jupyter-book build --html` to generate the documentation
4. Run `jupyter-book start` to run a local server to preview changes
